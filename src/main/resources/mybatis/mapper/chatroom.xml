<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.ChatRoomDao">

  <resultMap id="ChatRoomMap" type="com.example.demo.dto.ChatRoom">
    <id property="chatroomId" column="CHATROOM_ID"/>
    <result property="chatuser1Id" column="CHATUSER1_ID"/>
    <result property="chatuser2Id" column="CHATUSER2_ID"/>
    <result property="requestedBy" column="REQUESTED_BY"/>
    <result property="chatroomStatus" column="CHATROOM_STATUS"/>
    <result property="updatedAt" column="UPDATED_AT"/>
    <result property="createdAt" column="CREATED_AT"/>
  </resultMap>

  <select id="selectById" parameterType="int" resultMap="ChatRoomMap">
  SELECT * FROM CHATROOMS WHERE CHATROOM_ID = #{roomId}
</select>

  <!-- 사용자 두 명 조합으로 방 찾기: (min, max) 정규화 -->
 <select id="selectPairRoom" resultMap="ChatRoomMap">
  SELECT *
  FROM CHATROOMS
  WHERE (CHATUSER1_ID = LEAST(#{u1}, #{u2})
     AND CHATUSER2_ID = GREATEST(#{u1}, #{u2}))
    AND CHATROOM_STATUS IN ('P','A')
  ORDER BY (CASE CHATROOM_STATUS WHEN 'A' THEN 0 ELSE 1 END), CREATED_AT DESC
  FETCH FIRST 1 ROWS ONLY
</select>

  <insert id="insert" parameterType="com.example.demo.dto.ChatRoom">
    <selectKey keyProperty="chatroomId" resultType="java.lang.Integer" order="BEFORE">
      SELECT SEQ_CHATROOMS_CHATROOM_ID.NEXTVAL FROM dual
    </selectKey>

    INSERT INTO CHATROOMS
      (CHATROOM_ID, CHATUSER1_ID, CHATUSER2_ID, REQUESTED_BY, CHATROOM_STATUS)
    VALUES
      (#{chatroomId},
        LEAST(#{chatuser1Id}, #{chatuser2Id}),
        GREATEST(#{chatuser1Id}, #{chatuser2Id}),
        #{requestedBy},
        #{chatroomStatus})
</insert>

  <update id="updateStatus">
  UPDATE CHATROOMS
  SET CHATROOM_STATUS = #{status}, UPDATED_AT = SYSTIMESTAMP
  WHERE CHATROOM_ID = #{roomId}
</update>

</mapper>
