<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.ChatRoomDao">

  <resultMap id="ChatRoomMap" type="com.example.demo.dto.ChatRoom">
    <id     property="chatroomId"    column="CHATROOM_ID"/>
    <result property="chatuser1Id"   column="CHATUSER1_ID"/>
    <result property="chatuser2Id"   column="CHATUSER2_ID"/>
    <result property="requestedBy"   column="REQUESTED_BY"/>
    <result property="chatroomStatus" column="CHATROOM_STATUS"/>
    <result property="updatedAt"     column="UPDATED_AT"/>
    <result property="createdAt"     column="CREATED_AT"/>
  </resultMap>

  <select id="selectById" resultMap="ChatRoomMap">
    SELECT *
    FROM CHATROOMS
    WHERE CHATROOM_ID = #{roomId}
  </select>

  <select id="selectPairRoom" resultMap="ChatRoomMap">
    SELECT *
    FROM CHATROOMS
    WHERE CHATUSER1_ID = #{u1}
      AND CHATUSER2_ID = #{u2}
      AND CHATROOM_STATUS IN ('P','A')
  </select>

  <insert id="insert" parameterType="com.example.demo.dto.ChatRoom" useGeneratedKeys="false">
    INSERT INTO CHATROOMS(
      CHATROOM_ID, CHATUSER1_ID, CHATUSER2_ID, REQUESTED_BY, CHATROOM_STATUS, UPDATED_AT, CREATED_AT
    ) VALUES (
      SEQ_CHATROOMS_CHATROOM_ID.NEXTVAL,
      #{chatuser1Id},
      #{chatuser2Id},
      #{requestedBy},
      #{chatroomStatus},
      SYSTIMESTAMP, SYSTIMESTAMP
    )
  </insert>

  <update id="updateStatus">
    UPDATE CHATROOMS
       SET CHATROOM_STATUS = #{status},
           UPDATED_AT = SYSTIMESTAMP
     WHERE CHATROOM_ID = #{roomId}
  </update>

</mapper>
