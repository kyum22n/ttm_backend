<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.LikeDao">
  
    <!-- 좋아요 등록(Pet) -->
    <insert id="insertLikeToPet">
        <selectKey keyProperty="likeId" resultType="Integer" order="BEFORE"> 
            select seq_likes_like_id.nextval from dual 
        </selectKey> 

        insert into likes ( like_id, user_id, pet_id, created_at ) 
        values ( #{likeId}, #{userId}, #{petId}, SYSTIMESTAMP ) 
    </insert>
    
    <!-- 좋아요 등록(Post) -->
    <insert id="insertLikeToPost">
        <selectKey keyProperty="likeId" resultType="Integer" order="BEFORE"> 
            select seq_likes_like_id.nextval from dual 
        </selectKey> 

        insert into likes ( like_id, user_id, post_id, created_at ) 
        values ( #{likeId}, #{userId}, #{postId}, SYSTIMESTAMP ) 
    </insert>

    <!-- 좋아요 중복 체크(Pet) -->
    <select id="selectLikeFromPet" resultType="int">
        select COUNT(*)
        from likes
        where pet_id = #{petId} and user_id = #{userId}
    </select>
    
    <!-- 좋아요 중복 체크(Post) -->
    <select id="selectLikeFromPost" resultType="int">
        select COUNT(*)
        from likes
        where post_id = #{postId} and user_id = #{userId}
    </select>

    <!-- 좋아요 취소(Pet) -->
    <delete id="deleteLikeFromPet">
        delete from likes
        where pet_id = #{petId} and user_id = #{userId}
    </delete>
    <!-- 좋아요 취소(Post) -->
    <delete id="deleteLikeFromPost">
        delete from likes
        where post_id = #{postId} and user_id = #{userId}
    </delete>

</mapper>
